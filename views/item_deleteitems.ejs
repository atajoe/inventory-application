<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title><%= title %></title>
</head>
<body>
    <%- include("./partials/sidebar.ejs") %>
    <div class="master-content">
        <% if (itemInstances.length > 0) { %>
            <h1>Delete Item: <%= item.title %></h1>
            <h3>Description: <%= item.description %></h3>
            <h3><a href="/catalog/category/<%=item.category[0]._id%>">Category</a>: <%= item.category[0].title %></h3>
            <br>
            <h2>Delete the following copies/item instances before attempting to delete this item.</h2>
            <h1>Copies</h1>
            <% itemInstances.forEach(instance => { %>
             <div>Id: <a href="/catalog/iteminstance/<%=instance._id%>"><%=instance._id %></a> - <%= instance.condition %> <%= instance.price %></div>
            <% }) %>
        <% } else { %>
            <h1>Are you sure you want to delete this item?</h1>
            <h1>Delete Item: <%= item.title %></h1>
            <h3>Description: <%= item.description %></h3>
            <h3><a href="/catalog/category/<%=item.category[0]._id%>">Category</a>: <%= item.category[0].title %></h3>
            <form action="/catalog/item/delete/<%=item._id%>" method="POST">
                <input type="hidden" name="itemid", required="true", value=<%=item._id%>>
                <button>Delete</button>
            </form>
        <% } %>
    </div>
</body>
</html>